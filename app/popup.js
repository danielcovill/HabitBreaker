window.browser = (function () {
  return window.msBrowser ||
    window.browser ||
    window.chrome;
})();

document.addEventListener("DOMContentLoaded", function () {
	//get the current tab's page
	//check the page against the blacklist
	
	//update visual elements on the popup to show/hide items and update text
});